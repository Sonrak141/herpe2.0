/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com', 'images.unsplash.com', 'www.google.com', 'www.epicos.com'],
    },
}

module.exports = nextConfig
